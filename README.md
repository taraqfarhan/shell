# shell
Building my own POSIX compliant shell that's capable of interpreting shell commands, running external programs and builtin commands like cd, pwd, echo and more. The shell also supports command parsing, REPLs, builtin commands, and more.
